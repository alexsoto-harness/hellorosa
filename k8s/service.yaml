apiVersion: v1
kind: Service
metadata:
  name: hello-rosa-harness
  labels:
    app: hello-rosa-harness
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: hello-rosa-harness
